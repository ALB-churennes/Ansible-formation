[project]
name = "ansible-formation"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "ansible>=12.2.0",
    "ansible-lint>=25.11.0",
    "ruamel.yaml",
    "ruamel.yaml.clib",
    "ansible-compat>=4.1",
]
